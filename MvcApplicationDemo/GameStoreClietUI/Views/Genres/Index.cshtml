@model IEnumerable<GameStoreClietUI.Models.GenreCreateViewModel>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<div class="container">
    <div class="text-center my-margin">
        <p>
            @Html.ActionLink("Create new genre", "Create", null, new { @class = "btn btn-warning" })
        </p>

    </div>
    <div id="developers">
        @{
            Html.RenderPartial("Partial/GenresPartial", Model);
        }
    </div>
</div>